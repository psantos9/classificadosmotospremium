{
  "name": "@cmp/shared",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "migrations:init": "npm run migrations:generate && cp ./0001_seed_data.sql ./migrations",
    "migrations:generate": "npx drizzle-kit generate",
    "type-check": "npx tsc --noEmit"
  },
  "dependencies": {
    "drizzle-zod": "^0.6.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1"
  }
}
