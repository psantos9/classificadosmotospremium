{
  "name": "@cmp/database",
  "type": "module",
  "version": "1.0.0",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "migrations:generate": "npx drizzle-kit generate",
    "migrations:commit:dev:remote": "npx wrangler@latest d1 migrations apply cmp-dev -e dev --remote"
  },
  "dependencies": {
    "drizzle-zod": "^0.6.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1"
  }
}
